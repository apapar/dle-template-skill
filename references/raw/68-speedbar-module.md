# Speedbar Module

*DLE documentation topic #68*

In this section, you can configure the display of the current location of the user on your website. This template uses only one tag, `{speedbar}`, which outputs the location itself.
